services:
  - type: web
    name: edutrack
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py wsgi:app
    envVars:
      - key: FLASK_ENV
        value: prod
      - key: PYTHON_VERSION
        value: 3.11.0
